
use std::thread;

///this time, the goal of the program is to get an input with timeout using multithreading.

fn main() {
    
}


fn read<T>() -> T {
    //first thread
    let prize = 0;
    let input = thread::scoped(move || {

        if p
    });

}
