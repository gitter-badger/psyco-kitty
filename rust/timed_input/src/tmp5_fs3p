
use std::thread;
use std::io;;

///this time, the goal of the program is to get an input with timeout using multithreading.

fn main() {
    
}


fn read<T>() -> T {
    //first thread
    let prize = Arc::new(Mutex::new(0))
    let input: &str = ""; 
    let input_t = thread::s(move || {
        io::stdin().read_line(&mut guess)
        .ok()
        .expect("Failed to read line");
        prize
    });

}
